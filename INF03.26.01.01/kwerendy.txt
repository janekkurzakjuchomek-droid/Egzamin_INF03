kw1 - SELECT marka, model, cena FROM `pojazdy` WHERE marka = "BM" ORDER BY cena DESC LIMIT 15
kw2 - SELECT AVG(cena) as "Średnia cena", MAX(cena) as "Maksymalna cena" FROM `pojazdy` WHERE model = "meta" ORDER BY cena DESC LIMIT 15
kw3 - SELECT marka, model, cena, kolory.nazwa, kolory.doplata FROM `pojazdy`
        INNER JOIN kolory ON pojazdy.kolor = kolory.id
        WHERE model = "alfa" 
kw4 - SELECT marka, model, cena FROM `pojazdy` ORDER BY RAND() LIMIT 2